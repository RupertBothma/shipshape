apiVersion: v1
kind: ConfigMap
metadata:
  name: app-vars
  annotations:
    config.kubernetes.io/local-config: "true"
data:
  ENV: test
  HOSTNAME: test.helloworld.shipshape.example.com
  TLS_SECRET_NAME: helloworld-test-tls
  CLUSTER_ISSUER: letsencrypt-staging
  MESSAGE: hello from test environment
